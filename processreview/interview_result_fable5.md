# 검토 인터뷰 결과 — Fable 5

> **작성:** Claude Fable 5 · 2026-08-18
> **근거 문서:** `processreview/` 의 검토서 3건(`process_review_opus5/solhigh/flash37.md`)과
> 인터뷰 질문서 3건(`interview_opus5/solehigh/flash37.md`)
> **진행 방식:** 6개 문서의 질문 약 80개를 중복 제거해 8라운드(총 30문항)로 사용자에게 직접 질문했고,
> 이 문서는 그 **답변 전체와 확정 결정**을 기록한다.
> **이 문서가 이후 수정 세션의 유일한 의사결정 근거다.** 검토서·인터뷰의 권장안과 이 문서가
> 다르면 **이 문서가 우선한다** (사용자가 직접 답했다).

---

## 1. 한눈에 보는 확정 결정

| 영역 | 결정 |
|---|---|
| 수정 시점 | **전부 먼저 수정**하고 나서 제작(1단계) 시작 |
| 수정 담당 | **새 세션 Claude** (검토·인터뷰 문서를 작업 목록으로 전달) |
| 콘텐츠 후보 | **전 구간(DAY 01~30, 120세트) 3벌** — opus / sol / gemini 그대로 |
| 별도 검증 라운드 | **생략** — 생성 시 오류 나는 것만 재작업 |
| 프롬프트 금지어 검사기 | **만들지 않음** (승인 목록에서 제외) |
| 이미지 수급 | **Google Flow(nano banana 2 / lite) + Codex(GPT Image) 2가지**, 사용자 임의 판단 |
| 이미지 후보 | **2벌** — author 이름 `nanobanana2` / `gptimage2`, 폴더 구조는 현행(AI/세트) 유지 |
| 트림 버그 | **임계값만 0.55 → 0.40** (변 단위 재구현·FAIL 격리 없음) |
| OCR 필터 | **도입 안 함** |
| 세션 상한 | **코드 게이트 전부 제거** (`session-limits.mjs` 연결 해제) |
| 승격 교체 안전장치·해시 체인 | **둘 다 안 함** (승인 목록에서 제외) |
| TTS preflight·캐시 버전 관리 | **둘 다 안 함** |
| 낭독 속도 | **현재 수용** — report 에 실효 단어/초만 기록 |
| 원격 저장소 | **https://github.com/qdwe93/voca_hackers_v2 (생성 완료)** — 범위는 §4 참조 |
| 미디어 별도 백업 | **안 함** (git 원격이 그 역할) |
| v1 저장소 | 그대로 두되, **v2 문서에서 v1 연결고리(언급)를 전부 제거** |
| 수정 후 검증 | **fixture + DAY01 1세트 end-to-end 스모크** (GPU 사용 승인됨) |

---

## 2. 라운드별 질문·답변 기록

### 1라운드 — 착수 방식

| 질문 | 답변 |
|---|---|
| 치명적 버그 9건 + 문서 오타 17건 일괄 승인? | **전부 승인** — 단, 이후 라운드에서 3건 번복(§3 참조) |
| 수정 시점 | **전부 먼저 수정** (단계별 선행이 아니라 일괄 완료 후 제작 시작) |
| 수정 담당 | **새 세션 Claude** — 검토·수정 분리 원칙 유지 |
| 미답 항목 처리 | **전부 물어봐** → 8라운드에 걸쳐 전부 질문 완료 |

### 2라운드 — 1단계 콘텐츠

| 질문 | 답변 |
|---|---|
| 콘텐츠 후보 벌 수·범위 | **전 구간 3벌** (DAY 01~05 시험 후 축소안을 채택하지 않음) |
| 사용할 AI | **그대로** — Claude Opus(`opus`) / GPT Sol(`sol`) / Gemini(`gemini`) |
| 축소 판단 기준 | **사람이 보고 결정** (전 구간 3벌이므로 사실상 해당 없음) |
| 정의 낭독 속도 | **현재 수용** — 상수 변경·정의 단어 축소 없음. `audio_report.json` 에 실효 단어/초 기록만 추가 |

### 3라운드 — 검증 절차 (사용자가 범위를 크게 축소)

| 질문 | 답변 |
|---|---|
| 검증을 승격의 기계적 조건으로? | **사람 절차로만** — 스크립트는 검증 여부를 보지 않음 |
| 검증 보고서 저장 위치 | 후보 옆 MD+JSON (권장 선택) — 단, 아래 결정으로 사실상 사용 안 함 |
| 검증 시점 | **"검증 필요없어. 나중에 생성할때 오류나는것만 다시 진행할꺼야."** → 별도 검증 AI 라운드 자체를 생략 |
| 프롬프트 금지어 검사 세부 | **"금지어 검사 필요 없어. 빼."** → 검사기를 만들지 않음 |

> 이 라운드로 치명적 1번(금지어 검사기)이 승인 목록에서 빠졌다. `WHY.md:57` 의
> "검사기가 막는다" 문구는 사실과 다르므로 **문서 쪽을 사실에 맞게 수정**한다.
> `요청문/3_검증.txt` 는 삭제하지 않고 남겨 두되 필수 절차가 아니다.

### 4라운드 — 2단계 이미지

| 질문 | 답변 |
|---|---|
| 주 수급 도구 | **Google Flow(nano banana 2 또는 nano banana 2 lite) + Codex, 2가지.** 사용자가 임의로 판단. 이미지 전체를 각각의 AI 로 2벌 이상 생성 후 판단할 수도 있음. **이미지 폴더에서 어떤 AI 를 썼는지 구분되는 구조 필요** (→ 8라운드에서 현행 author 상위 구조로 확정) |
| 이미지 후보 벌 수 | **2벌** — nano banana 2, Codex(GPT Image) |
| 트림 버그 수정 방식 | **임계값만 0.40** — 변 단위 재구현(sol 안)과 FAIL 격리(opus 안)는 채택하지 않음 |
| OCR 1차 필터 | **도입 안 함** |

### 5라운드 — 선별·QA

| 질문 | 답변 |
|---|---|
| author 폴더 이름 | **`nanobanana2` / `gptimage2`** |
| 사람 선별 범위 | **전부 보되 경고 우선** — 세트별 대지는 다 보고, 임포터 경고 칸을 먼저 확인 |
| 렌더 후 육안 QA 표본 | **10세트당 1개 + 문제 발견 시 그 DAY 4세트 전수 확대** (ffprobe·contact sheet 는 120세트 전부 자동) |
| QA 프레임 추출 시점 | **각 단어 블록 +5초(학습) / +13초(예문)** |

### 6라운드 — 정합성·상한·운영

| 질문 | 답변 |
|---|---|
| TTS preflight + 캐시 버전 관리 | **둘 다 안 함** |
| 승격 교체 거부(--replace 안전장치) + 해시 체인 | **둘 다 안 함** → 치명적 9번이 승인 목록에서 빠짐 |
| 세션 상한 | **상한 전부 제거** — 코드 게이트를 남기지 않는다 |
| handover 동시 수정 문제 | **author 별 run_report** — 각 AI 는 `content/candidates/<author>/run_report.md` 에 기록, 승격 시 사람이 한 번만 `handover.md` 에 요약 |

### 7라운드 — 문서·저장소·백업

| 질문 | 답변 |
|---|---|
| WHY.md 반영 | **수정 세션에서 일괄** — 바뀐 결정을 날짜와 함께 갱신 |
| 원격 저장소 | **https://github.com/qdwe93/voca_hackers_v2 를 이미 만들었음.** "결과 산출물은 1일차만 전수 포함(용량 이슈 발생 시 동영상은 제외). 나머지는 동영상은 제외. 프로젝트 관련 코드나 프롬프트, 세팅 등의 파일은 전부 포함." |
| 미디어 별도 백업 | **안 함** |
| v1 저장소 | **그대로 둠. "앞으로 v1 은 전혀 무관하므로 아예 연결고리가 없도록 대상에서 뺄 것."** → v2 문서·요청문의 v1 언급을 제거 |

### 8라운드 — 구조·git 범위·스모크

| 질문 | 답변 |
|---|---|
| 이미지 후보 폴더 구조 | **현행 유지** — `content/image-candidates/<author>/<세트>/` (author 상위). 4라운드의 "AI 구분" 요구는 이 구조로 충족 |
| GitHub 에 올릴 산출물 범위 | DAY01 전수(이미지+오디오+영상, 용량 이슈 시 영상 제외), 나머지 DAY 는 영상만 제외하고 이미지·오디오 포함, **후보(선별 전 2벌)도 포함** (≈14GB 용량 주의는 고지했고 사용자가 선택) |
| 수정 후 검증 수준 | **fixture + DAY01 1세트 스모크** — 콘텐츠→이미지→TTS→렌더까지 통과 확인 (GPU 사용 포함) |

---

## 3. 최종 승인된 수정 목록 (번복 반영)

1라운드에서 일괄 승인된 26건 중, 이후 라운드의 구체적 답변으로 **3건이 제외·변경**됐다.

### 코드 수정 — 승인 유지 (7건)

| # | 무엇 | 확정 방식 |
|---|---|---|
| 1 | `req.txt` 교체 | v2 안전 기준만 남긴 문서로 교체 (초6 수준·폭력/공포/상표 금지·안전 치환 예시). 구버전은 참조 제거 |
| 2 | 문두 단어를 사람 이름으로 오인 (`Every`, `Monday`) | 문장 첫 토큰을 이름 후보에서 제외 |
| 3 | 임포터 단색 밴드 트림 | **`TRIM_MIN_AREA_RATIO` 0.55 → 0.40 만** (그 외 변경 없음) |
| 4 | `generate-images-agy.mjs` 중복 생성 | `--author` 시 후보·inbox 도 확인 (주 수급이 아니게 됐으므로 우선순위 낮음) |
| 5 | TTS 배치 예외 격리 | 세트별 try/except, 실패 수집 후 계속, 마지막에 요약 표. 전역 오류만 즉시 중단 |
| 6 | `status.mjs` 4단계 반영 | nextPhase 를 3단계 선별/4단계 렌더로 정확히 분기. `--verify` 옵션 추가 |
| 7 | 렌더 후 자동 QA | 렌더 직후 ffprobe(길이·프레임·스트림) 검사, 실패 MP4 는 완료로 세지 않음. `remotion still` 로 +5초/+13초 프레임 20장 추출 → contact sheet 자동 생성 |

### 코드 수정 — 사용자 결정으로 제외 (2건)

| 무엇 | 처리 |
|---|---|
| 프롬프트 금지어 검사기 (치명적 1번) | **만들지 않는다.** 대신 `WHY.md:57` "검사기가 막는다" 문구를 사실대로 수정 |
| 승격 교체 안전장치·해시 체인 (치명적 9번) | **만들지 않는다.** 재작업 시 정합성은 사람이 관리 |

### 코드 수정 — 방향 전환 (1건)

| 무엇 | 처리 |
|---|---|
| 세션 상한 | 연결 보강이 아니라 **전부 제거** — `pick-images`·`generate-images-agy` 의 `refuseIfTooBig` 호출 삭제, `session-limits.mjs` 미사용화(또는 삭제), 문서의 "스크립트가 거부한다" 문구 전부 삭제 |

### 문서·기타 정리 (승인 유지, 결정 반영해 확장)

1. `prompts/P4:1` 제목 `# P3` → `# P4`
2. `요청문/1:18` "필드 9개" → 10개
3. `prompts/P2:50`, `prompts/P4:38` 의 실행 불가 명령 수정
4. `PLAN.md` 192.85초 표기 → 영상 192.80초 / ffprobe 실측 192.853초 구분
5. `PLAN.md:146` 정의 8.0초 → 화면 8.0초 / 음성 예산 6.35초 병기
6. `build_set_audio.py:169` legacy Aiden/Ryan 문구 삭제
7. `status.mjs` author 첫 글자 표시 → 전체 이름
8. `content/candidates/` 에 `.gitkeep`
9. `sharp` 를 dependencies 로 이동
10. `normalize-generated-image.mjs` 삭제
11. `import-images.mjs` 무인자 실행이 author 폴더를 재귀하는 문제 → 하위 폴더 발견 시 거부하고 `--author` 안내
12. `emit-request.mjs --set <세트>` 신규 (1b 요청문 자동 생성)
13. `audio_report.json` 에 `effectiveWordsPerSecond` 기록 추가
14. P1·요청문의 handover 기록 지시 → `content/candidates/<author>/run_report.md` 방식으로 변경
15. **v1 언급 제거** — `AGENTS.md`·`PLAN.md`·`WHY.md`·요청문의 v1(`hackers_video_project`) 참조를 제거 (구프로젝트 `junbe_study` 경고는 유지 여부를 수정 세션이 판단)
16. **`.gitignore` 재작업 + 원격 연결** — §4 의 git 정책 반영, `origin` 을 `https://github.com/qdwe93/voca_hackers_v2` 로 연결
17. `WHY.md` 일괄 갱신 — 이번 결정으로 근거가 바뀐 항목(2·5·6·7번 등)에 2026-08-18 날짜로 반영

---

## 4. git 정책 (사용자 지시 원문 기준)

> "결과 산출물은 1일차만 전수 포함. (용량이슈 발생시 동영상은 제외) 나머지는 동영상은 제외.
> 프로젝트 관련 코드나 프롬프트, 세팅 등의 파일은 전부 포함." + 후보도 포함(8라운드)

| 대상 | 추적 여부 |
|---|---|
| 코드·프롬프트·문서·세팅 전부 | O |
| `content/candidates/` (콘텐츠 후보 3벌) | O (텍스트) |
| `content/image-candidates/` (이미지 후보 2벌, ≈14GB) | **O** (용량 주의 고지됨) |
| `remotion/public/days/*/images/`, `audio/` | **전 DAY 포함** |
| `remotion/out/*.mp4` (영상) | **DAY01 만 포함** (용량 이슈 시 DAY01 도 제외), 나머지 DAY 제외 |
| 원격 | `https://github.com/qdwe93/voca_hackers_v2` (생성 완료, 연결 필요) |

미디어 별도 백업은 하지 않는다. v1 저장소는 손대지 않는다.

---

## 5. 검토서 권장과 달라진 선택 (기록)

아래는 세 검토서의 권장과 다른 사용자 결정이다. 재론하지 않는다. 나중에 문제가 실제로
생기면 그때 이 목록을 근거로 재검토한다.

| 항목 | 검토서 권장 | 사용자 결정 | 남는 위험 (고지됨) |
|---|---|---|---|
| 별도 검증 AI 라운드 | 실시 (DAY 5개마다) | 생략 | IPA·의미 오류를 기계·타 AI 가 잡지 않음. 사람 승격 판단이 유일한 방어선 |
| 금지어 검사기 | 에러로 차단 | 안 만듦 | 생성 AI 가 규칙을 어긴 프롬프트가 그대로 통과 |
| 세션 상한 | 연결 보강 | 전부 제거 | 대량 실행 중단 시 복구 비용 (v1 실사고 근거였음) |
| 승격 교체 안전장치 | --replace 거부 | 안 만듦 | 재작업 시 문장·그림·음성 어긋남을 사람이 직접 관리 |
| TTS preflight·캐시 버전 | 추가 | 안 함 | 승격 후 파일 변경·환경 변경 시 불일치 미검출 |
| OCR 필터 | 배지 표시 | 안 함 | 글자 혼입 선별을 전량 육안으로 |
| 콘텐츠 시험 후 축소 | DAY 01~05 시험 | 전 구간 3벌 | 비용 3배 (품질 교차 검증은 극대화) |
| 낭독 속도 | 샘플 청취 후 결정 | 현재 수용 | 정의 낭독이 목표(2.6단어/초)보다 빠른 상태 유지 |

---

## 6. 다음 세션 착수 지시 (요약)

새 세션 Claude 에게 이 문서를 주고 다음 순서로 진행한다.

1. **§3 의 수정 전부** (코드 7건 + 문서·기타 17건, 제외 2건·전환 1건 반영)
2. **§4 의 git 정책** — `.gitignore` 재작업, 원격 연결, 첫 push
3. **fixture + DAY01 1세트 스모크** — 콘텐츠→이미지→TTS→렌더 (GPU 사용 승인됨)
4. 스모크 통과 후 **1단계 착수**: `요청문/1` 을 opus / sol / gemini 에게 발송, 전 구간 3벌
5. 이후: 이미지 2벌(`nanobanana2`, `gptimage2`) → 사람 선별(경고 우선, 전부 육안) →
   TTS·렌더 무인 배치 → 자동 QA 120세트 + 육안 표본 10세트당 1개
