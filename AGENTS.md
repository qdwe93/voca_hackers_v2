# 해커스 보카 v2 — 세션 라우팅

> ## ⚠️ 작업 폴더를 먼저 확인한다
>
> **이 프로젝트의 작업 폴더는 `C:\Workspaces\english\hackers_video_project_v2` 다.**
>
> 읽기 전용이라 **열지도 쓰지도 않는 폴더**:
> - `C:\Workspaces\english\hackers_video_project` — v1(있다면). 사양은 같지만 절차가 다르다.
>   **v2 는 v1 없이 단독으로 완전히 동작한다.** v1 이 없어도 아무 문제 없다
> - `C:\Workspaces\junbe_study\voca_video_plan` — 구프로젝트. 스키마 v2 이고 TTS 자동화가
>   없다. 여기를 근거로 "TTS 자동화가 없다"고 판단하면 틀린 결론이다 (실제 사고 사례)
> - `C:\Workspaces\tts\qwen3_tts_1.7b_base` — 모델·voices·`.venv` 참조용
>
> **이미지·음성·영상은 git 에 없다(로컬 전용).** 저장소에 이미지가 없다고 "아직 안
> 만들었다"고 판단하지 마라. 진행 상황의 유일한 근거는
> `node remotion/scripts/status.mjs` 다.

**왜 이런 구조인지는 [`WHY.md`](WHY.md) 에 있다.** 결정마다 근거·버린 대안·바꿔도 되는
조건을 적어 뒀다. 절차를 바꾸자는 판단을 하기 전에 반드시 먼저 읽는다.
프로세스 검토만 하는 세션은 `요청문/4_프로세스_검토.txt` 를 쓴다.

30일(120세트)을 **단계별로 통째로** 진행한다. DAY 하루치씩 왕복하지 않는다.

| 단계 | 무엇 | 누가 | 1회 상한 | 문서 |
|---|---|---|---|---|
| **1** | 콘텐츠 + 이미지 프롬프트 | 외부 AI 여럿이 각각 | **DAY 10개** | `prompts/P1_콘텐츠_이미지프롬프트.md` |
| 1-선별 | 후보 비교 → 승격 | 로컬 + 사람 | DAY 10개 | `compare-candidates` → `promote-candidate` |
| **2** | 이미지 생성 (AI별로 각각) | 아무 도구 | **80장 = DAY 1개** | `prompts/P2_이미지_생성.md` |
| **3** | 이미지 선별 (AI별 후보 → 최종) | 사람 | **20세트 = DAY 5개** | `prompts/P3_이미지_선별.md` |
| **4** | TTS + 렌더 | 로컬 무인 배치 | 120세트(무인) | `prompts/P4_오디오_렌더.md` |

**단계를 섞어서 한 번에 시키지 않는다.** 1·2·3단계는 스크립트가 상한을 넘는 실행을
거부한다(`remotion/scripts/session-limits.mjs`). 4단계만 통짜로 돌린다.

복붙용 요청문은 `요청문/` 폴더에 있다:
`1_콘텐츠_이미지프롬프트_생성.txt`, `1b_저장소없이_생성.txt`, `2_이미지_생성.txt`, `3_검증.txt`.
**검증은 만든 AI 가 아니라 다른 AI 에게 시킨다.**

## 복붙용 요청 문구

전문은 `요청문/` 의 텍스트 파일을 그대로 복사한다. 아래는 어떤 파일을 언제 쓰는지다.

| 상황 | 파일 |
|---|---|
| 콘텐츠·이미지 프롬프트 생성 (저장소 접근 O) | `요청문/1_콘텐츠_이미지프롬프트_생성.txt` |
| 콘텐츠·이미지 프롬프트 생성 (웹 챗, 저장소 접근 X) | `요청문/1b_저장소없이_생성.txt` |
| 이미지 생성 (에이전트 / 웹 UI 둘 다) | `요청문/2_이미지_생성.txt` |
| 산출물 검증 (**만든 AI 가 아닌 다른 AI**) | `요청문/3_검증.txt` |
| 프로세스 검토 (새 세션에서 설계를 점검) | `요청문/4_프로세스_검토.txt` |

### 공통 서두 (저장소에 접근하는 AI 에게는 이 4줄을 먼저 붙인다)

```text
작업 폴더는 C:\Workspaces\english\hackers_video_project_v2 하나뿐이다.
다른 폴더는 있더라도 읽기 전용이다. 열지도 말고 쓰지도 마라.
시작 전에 WHY.md, AGENTS.md, PLAN.md, 해당 단계의 prompts/P*.md, handover.md 를 읽어라.
이미지·음성·영상은 git 에 없다. 진행 상황은 node remotion/scripts/status.mjs 로만 판단하라.
```

### 단계별 한 줄 요약

- **1단계** — `DAY 01~05 의 20세트 words.json + image_prompts.md 를
  content/candidates/<이름>/ 에 만들고 validate-content --author <이름> 을 통과시켜라.
  이미지·TTS·렌더는 하지 마라.` (DAY 10개 초과 금지)
- **2단계** — `DAY 01 의 이미지를 만들어 inbox/<이름>/ 에 목표 파일명으로 넣고
  import-images.mjs --author <이름> 으로 반입해라. 프롬프트를 늘리지 마라.`
  (1회 80장 초과는 스크립트가 거부)
- **3단계** — 사람이 한다. `make-image-sheets.mjs --day 01` 로 AI별 대지를 만들어 비교하고
  `pick-images.mjs --author <이름> --set <세트>` 로 올린다 (1회 20세트 상한)
- **4단계** — `밀린 오디오와 렌더를 전부 돌려라. build_set_audio.py --all →
  check-assets.mjs → render-batch.mjs. 실패 세트는 건너뛰고 마지막에 모아 보고해라.
  격자·속도 상수는 바꾸지 마라.`

### 검증을 시킬 때

생성과 검증은 **다른 AI 에게** 시킨다. 만든 쪽은 자기 실수를 못 본다.
검증 AI 에게는 수정 권한을 주지 않는다 — `요청문/3_검증.txt` 는 "고치지 말고 목록으로만
보고" 를 명시하고 보고 형식(반드시 고쳐야 함 / 고치면 좋음 / 이상 없음)을 고정한다.
수정은 원래 만든 AI 나 사람이 한다.

## 이렇게 요청하면 안 된다

| 나쁜 요청 | 왜 |
|---|---|
| `이 폴더 읽고 알아서 만들어줘` | 폴더를 잘못 짚으면 통째로 헛돈다 (실제 사고) |
| `DAY 01~30 전부 한 세션에 끝까지` | 단계마다 상한이 다르고 스크립트가 거부한다. 1단계 DAY 10개 / 2단계 80장 / 3단계 20세트 |
| `프롬프트 만들고 이미지까지 만들고 렌더까지 해줘` | 단계를 섞지 않는다. 1·2·3단계는 사람이 사이에 낀다 |
| `이미지 비율이 안 맞으니 프롬프트에 16:9라고 써줘` | v2 는 정반대다. 비율 지시가 실패 원인이었다. 규격은 임포터가 맞춘다 |
| `보강 문구를 전부에 넣어줘` | 프롬프트가 길수록 약한 모델이 무너진다. 실패한 자산에만 붙인다 |
| `네가 만든 거 네가 검증해줘` | 만든 AI 는 자기 기준으로 본다. 검증은 다른 AI 에게 시킨다 |
| `목소리를 더 또박또박하게` | 격자·속도는 확정값이다. 바꾸려면 근거와 함께 명시적으로 지시한다 |

## 전 세션 공통 규칙

- 기존 산출물(`words.json`, `images/`, `narration.mp3`, `out/*.mp4`)을 덮어쓰지 않는다.
  재작업은 사용자가 대상과 이유를 지정했을 때만 한다
- 게이트 순서: `validate-content` → 승격 → 이미지 → `check-assets` → 렌더
- 타이밍 격자(1.5/1.5/8/8초), 화자 순환(Zephyr→Liam→Erinome→Charon), 속도 보정 상수,
  예문 화면 1.5초 선행은 사용자 지시 없이 바꾸지 않는다
- `words.json` 콘텐츠 생성은 최상위 모델로 한다 (IPA·정의·예문은 기계 검증 불가)
- 파이썬은 반드시 `C:\Workspaces\tts\qwen3_tts_1.7b_base\.venv\Scripts\python.exe`
- 한 단계를 3회 실패하면 중단하고 `handover.md` 에 기록한다. 무한 재시도 금지
- 세션 종료 시 `handover.md` 맨 위에 완료 내역·진행률·다음 시작 프롬프트를 남긴다
