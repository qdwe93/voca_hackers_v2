# WHY — 이 프로젝트가 왜 이렇게 만들어졌나

`PLAN.md`가 실행 절차라면 이 문서는 결정의 근거와 변경 조건을 기록한다. 근거에 “실측”이라고
적은 값은 이 PC에서 직접 확인한 수치다. 2026-08-18 프로세스 인터뷰의 최종 결정은
`processreview/interview_result_fable5.md`에 있다.

## 0. 목표

`hackers.csv`의 1,200단어를 한국 초등 6학년용 영상 120편으로 만든다. 영상 계산 길이는
192.80초이며 MP4 컨테이너 실측은 192.853초다. 아이가 그대로 외우므로 발음·뜻·예문의
정확도는 자동화보다 우선한다.

무관한 `C:\Workspaces\junbe_study\voca_video_plan`은 외부 TTS 산출물을 넣는 별도 구조다.
이 프로젝트의 TTS 자동화 여부를 판단하는 근거로 읽지 않는다. 외부 의존은 TTS 모델 폴더와
선택적 `agy.exe`뿐이다.

## 1. 왜 30일을 단계별로 통째로 진행하나

**결정.** 콘텐츠 → 이미지 생성 → 이미지 선별 → TTS·렌더를 각각 30일 전체에 대해 끝낸다.

**근거.** 단계마다 필요한 능력과 도구가 다르고, 단계 경계가 파일 산출물이다. DAY마다
모델 전환·문서 재독·환경 재구축을 반복할 이유가 없다.

**버린 대안.** DAY 하나를 콘텐츠부터 렌더까지 완주하는 방식. 단, 파이프라인 변경 후
DAY01 한 세트를 끝까지 돌리는 스모크는 유효하다.

**바꿔도 되는 조건.** 이미지 생성·선별이 완전 자동화되어 단계 전환 비용이 사라질 때.

## 2. 왜 이미지 프롬프트를 한 문장으로 줄였나

**결정.** 장면 한 문장(권장 30단어, 하드 상한 45단어)과 고정 접미만 쓴다. 비율·해상도·
하단 여백·로고/워터마크 금지 반복·품질 수식어·특정 화풍은 요청에 넣지 않는다.
금지어 자동 검사기는 만들지 않는다(2026-08-18 사용자 확정). 작성자와 사람이 확인한다.

**근거.** nano banana 2 실측에서 지시가 길수록 피사체 잘림, 단색 밴드, 글자·로고 혼입이
늘었다. 약한 모델에는 장면만 맡기고 형식은 임포터가 책임지는 편이 안정적이었다.

**버린 대안.** 전량 보강 문구, 프롬프트에서 비율·해상도 강제, 특정 강한 모델만 사용.

**바꿔도 되는 조건.** 사용하는 이미지 모델 모두가 짧은 지시 없이도 비율·구도를 정확히
지키거나, 반대로 특정 실패가 반복되어 실패 자산별 `--escalate`로도 해결되지 않을 때.

## 3. 왜 규격 책임을 임포터로 옮겼나

**결정.** `import-images.mjs`가 단색 테두리 트림 → cover 크롭 → PNG 변환 → 의심 경고를
처리한다. 트림 최소 잔존 면적비는 2026-08-18에 0.55에서 **0.40**으로 낮췄다.

**근거.** 1024×1024에 단색 밴드가 붙은 이미지를 넣어 44% 테두리를 제거하고 1600×900으로
정상화한 실측이 있다. 0.55는 더 큰 밴드에서 트림을 되돌리는 경계 버그가 있어 0.40으로
수정했다. 예문 이미지는 하단 자막을 피하도록 북쪽 우선 크롭한다.

**버린 대안.** 백엔드마다 별도 변환 로직, 변 단위 트림 전면 재구현, 실패 파일 자동 격리.

**주의.** 의미 불일치·글자 혼입·무서운 장면은 임포터가 고치지 못하므로 사람이 전량 본다.

## 4. 왜 콘텐츠 3벌과 이미지 2벌을 만드나

**결정.** DAY01~30 콘텐츠를 `opus` / `sol` / `gemini` 3벌, 이미지를 `nanobanana2` /
`gptimage2` 2벌 만든다(2026-08-18 사용자 확정).

**근거.** IPA, 정의의 의미, 예문의 자연스러움, 그림의 의미 적합성은 형식 검사만으로 판정할
수 없다. 후보 간 차이가 의심 지점을 드러내며, 사람은 그 차이를 중심으로 판단할 수 있다.

**버린 대안.** 초반 일부만 3벌 만든 뒤 축소, AI별 역할 분담, 단일 이미지 백엔드.

**바꿔도 되는 조건.** 전 구간 결과에서 품질 차이 또는 비용 데이터가 충분히 쌓인 뒤 사용자가
후속 제작 정책을 다시 정할 때.

## 5. 왜 별도 검증 AI 라운드를 필수에서 뺐나

**결정(2026-08-18).** 생성 후 별도 검증 AI 라운드는 생략한다. `validate-content.mjs`의 형식
검증은 유지하며, 생성 오류가 발생한 산출물만 재작업한다. `요청문/3_검증.txt`는 선택 도구로
남긴다. 승격 시 사람의 비교 판단이 의미 정확도의 유일한 방어선이다.

**근거.** 전 구간 3벌을 사람이 비교하므로 별도 검증 비용을 추가하지 않겠다는 사용자 결정이다.

**남는 위험.** IPA·뜻·예문 오류를 별도 AI가 한 번 더 찾지 않는다. 실제 문제가 생기면
`요청문/3`의 읽기 전용 검증을 복원한다.

## 6. 왜 실행 상한을 제거했나

**결정(2026-08-18).** `session-limits.mjs`와 호출부를 제거했다. 1·2·3단계 모두 코드상 실행
상한이 없다.

**근거.** 전체 범위를 각 AI에 맡기고 파일 존재 기반으로 재개하는 운영을 사용자가 선택했다.
기존 상한은 일부 명령에만 연결되어 실제 보장과 문서가 달랐다.

**남는 위험.** 대량 실행이 중간에 끊기면 복구 비용이 커질 수 있다. 각 스크립트는 이미 있는
산출물을 건너뛰고 누락분만 재실행해야 한다.

**바꿔도 되는 조건.** 반복적인 중단으로 복구 비용이 실제로 발생해 사용자가 게이트 복원을
승인할 때.

## 7. 왜 Git에 미디어까지 넣나

**결정(2026-08-18).** 코드·문서·설정·콘텐츠 후보 3벌, 이미지 후보 2벌, 전 DAY 최종 이미지와
오디오를 추적한다. MP4는 DAY01만 추적하고 용량 문제가 생기면 DAY01 영상도 제외한다.
원격은 `https://github.com/qdwe93/voca_hackers_v2`다. 별도 미디어 백업은 만들지 않는다.

**근거.** 사용자가 원격 Git을 프로젝트와 산출물의 단일 백업으로 쓰기로 확정했다. 이미지
후보까지 포함하면 대용량 저장소가 된다는 위험을 고지한 뒤 선택했다.

**남는 위험.** 후보 이미지와 오디오는 수 GB 이상이어서 일반 Git push 한도나 호스팅 정책에
걸릴 수 있다. 실제 push 실패 시 MP4 제외만으로 부족할 수 있으며 그때 추적 방식을 재결정한다.

## 8. 왜 타이밍·화자·속도 상수를 못 건드리나

**결정.** 19초 격자(1.5/1.5/8/8), 예문 화면 1.5초 선행, narration 190.0초, 화자 순환
Zephyr→Liam→Erinome→Charon, 압축 하한 0.85, 감속 상한 1.2, 3.0→2.6단어/초 목표를
사용자 지시 없이 바꾸지 않는다.

**근거.** 120편이 같은 리듬이어야 하며 이미 실측 조정된 값이다. 정의 화면은 8.0초지만
실제 음성 예산은 6.35초다. 현재 속도는 수용하되 `audio_report.json`에
`effectiveWordsPerSecond`를 남겨 후속 판단 근거로 쓴다.

## 9. 왜 작업 폴더 경고가 문서 맨 위에 있나

**결정.** 모든 저장소 접근 요청에 정확한 작업 폴더를 쓴다.

**근거.** 무관한 구프로젝트에서 작업해 TTS 자동화가 없다는 잘못된 결론과 재사용 불가능한
산출물을 만든 실제 사고가 있었다. 에이전트의 보고는 자기가 연 폴더에 대해서만 참이다.

## 10. 검토했다 버린 대안

| 대안 | 이유 |
|---|---|
| 로컬 이미지 생성(ComfyUI 등) | 사용자 실측 품질 미달 |
| 안티그래비티 IDE를 MCP로 연결 | 서버 모드 없음. `agy` CLI는 별개 |
| `agy`로 TTS | 이미지 전용 |
| 외부 TTS+SRT로 타이밍 역추출 | 고정 슬롯 직접 합성이 더 결정적 |
| `words.json`에 author 필드 | strict 렌더 스키마가 거부. 폴더명이 author 역할 |
| 프롬프트 전량 보강 | 약한 모델에서 실패 증가 |
| OCR 필터 | 도입하지 않기로 확정 |
| TTS preflight·캐시 버전 | 도입하지 않기로 확정 |
| 승격 교체 안전장치·해시 체인 | 도입하지 않기로 확정. 정합성은 사람이 관리 |

## 11. 남은 운영 위험

- 이미지 4,800장(2벌) 생성 시간과 도구 쿼터는 전 구간 실측 전이다.
- 이미지 선별은 대지 전량 육안이며 OCR 자동 필터가 없다.
- TTS·렌더 120세트 총 시간은 1세트 실측 기반 추정치다.
- 대용량 미디어를 일반 Git 원격에 올리는 정책은 첫 실제 대용량 push에서 한계가 드러날 수 있다.
- 별도 검증 라운드와 정합성 해시가 없어 사람 승격·재작업 관리가 중요하다.

## 12. 프로세스 검토 방법

이 문서 → `PLAN.md` → `AGENTS.md` → `prompts/P1`~`P4` → `handover.md` 순으로 읽고
`node remotion/scripts/status.mjs --verify`로 실제 상태를 확인한다. 결정을 바꾸려면 기존 근거를
무엇으로 대체할지와 새 실측을 함께 기록한다. 변경은 날짜와 함께 이 문서와 `handover.md`에
남긴다. 복붙용 요청문은 `요청문/4_프로세스_검토.txt`다.
