# WHY — 이 프로젝트가 왜 이렇게 만들어졌나

새 세션(Claude·Codex 등)이 **프로세스 검토부터 시작할 때 제일 먼저 읽는 문서**다.
`PLAN.md` 는 "무엇을 어떻게 하는가", 이 문서는 "왜 그렇게 정했는가"를 적는다.

각 결정은 **결정 / 근거 / 검토했다 버린 대안 / 바꿔도 되는 조건** 순으로 쓴다.
근거에 "실측" 이라고 적힌 것은 이 PC 에서 직접 돌려 확인한 수치다. 추측이 아니다.
**근거를 반박하지 않고 결정만 바꾸자고 하지 마라.** 반대로, 근거가 무너졌다면 결정은
바뀌어야 한다 — 그게 이 문서를 남기는 이유다.

---

## 0. 배경 — 이 프로젝트의 목표

`hackers.csv` 의 1,200단어(DAY 01~30 × 40단어)를 **한국 초등 6학년용 학습 영상 120편**
으로 만든다. 세트 = 10단어 = 영상 1편(192.85초). 사람 손을 최소화하되, **아이가 그대로
외우는 콘텐츠**이므로 발음·뜻·예문의 정확도는 자동화보다 우선한다.

이전 시도가 둘 있다.

| 프로젝트 | 상태 | 이 프로젝트와의 관계 |
|---|---|---|
| `junbe_study/voca_video_plan` | 구프로젝트, 스키마 v2 | 사용자가 외부 TTS 로 MP3+SRT 를 만들어 넣는 방식. **TTS 자동화가 없다.** 읽지 않는다 |
| `english/hackers_video_project` (v1) | DAY01·DAY18 완료(8/120) | 사양(타이밍·TTS·화면)은 그대로 계승. 진행 순서와 프롬프트 규칙만 바꿔 v2 가 됐다 |

v2 는 **v1 이 삭제돼도 단독으로 동작한다** (2026-08-17 전 구간 실측 확인).
외부 의존은 TTS 모델 폴더와 선택적 `agy.exe` 뿐이다.

---

## 1. 왜 30일을 "단계별 통짜" 로 진행하나 (v1 은 DAY 하루치씩이었다)

**결정.** 1단계(콘텐츠·프롬프트) → 2단계(이미지 생성) → 3단계(이미지 선별) →
4단계(TTS·렌더) 를 각각 30일 전체에 대해 끝내고 다음으로 넘어간다.

**근거.**
- v1 은 DAY 하나를 콘텐츠부터 렌더까지 끝내고 다음 DAY 로 갔다. 한 DAY 마다 모델 전환,
  세션 시작, 문서 재독, 컨텍스트 재구축 비용이 다시 붙었다. 120세트면 이 비용이 30번 든다.
- 단계마다 **필요한 능력과 도구가 다르다.** 콘텐츠는 최상위 언어모델, 이미지는 이미지
  도구와 쿼터, 렌더는 로컬 CPU/GPU 시간이다. 섞으면 비싼 세션이 싼 일을 하게 된다.
- 단계 경계가 곧 **파일 산출물**(`words.json` → `image_prompts.md` → `images/` →
  `out/*.mp4`)이라 세션 간에 컨텍스트를 넘길 필요가 없다.

**버린 대안.** DAY 단위 완주(v1 방식) — 위 이유로 폐기. 단, 시험용으로 DAY 1개만
끝까지 돌려보는 것은 여전히 유효하다(스모크).

**바꿔도 되는 조건.** 이미지 수급이 완전 자동화되고(쿼터 걱정 없이 2,400장) 선별이
필요 없어지면, 단계 분리의 이득이 줄어든다.

---

## 2. 왜 이미지 프롬프트를 한 문장으로 줄였나 ★ v2 의 핵심

**결정.** 프롬프트 = `<장면 묘사 영어 한 문장(30단어 권장/45 하드상한)>` + 고정 접미
(`Stylized 3D cartoon illustration, square. No text.` / 예문은 `wide. Main subject in the
upper two thirds.`). **비율·해상도·하단 여백·no logo·no watermark·no badge·조명/렌더
품질 수식어·특정 화풍은 금지**하고 검사기가 막는다.

**근거.**
- v1 프롬프트는 60~80단어였다: canonical prefix(화풍 수식어 12단어) + 장면 +
  금지문구 3종(무텍스트·무로고·`plain unbranded surfaces…`) + 하단 24% 여백 지시.
- 사용자 실측(**nano banana 2**): 지시가 길수록 무너졌다. 관측된 실패 3종 —
  ① 피사체가 잘림 ② 요청 비율을 **단색 밴드**로 채움 ③ 금지문구를 늘릴수록 오히려
  글자·로고가 섞임.
- v1 실측도 같은 방향이었다. 강화 금지문구를 전량에 붙인 DAY18 에서도 글자 혼입으로
  5장을 재생성했다(`11_prevent_sent` 는 2회). 즉 **긴 금지문구는 보험이 아니었다.**
- 약한 모델일수록 지시를 "따르려다" 화면 구성을 망친다. 그래서 **모델에게는 장면만
  시키고, 형식은 우리 코드가 책임진다.**

**버린 대안.**
- 프롬프트로 계속 통제 + 규격 어긋나면 재생성 → 재생성이 곧 비용이고, 약한 모델에서는
  재생성해도 같은 실패가 반복됐다.
- 강한 모델만 쓰기 → 도구 선택지가 좁아지고(Google Flow·nano banana 배제) 쿼터에 묶인다.

**바꿔도 되는 조건.** 쓰는 이미지 모델이 전부 비율·구도를 정확히 지키는 수준이 되면
접미의 `square`/`wide` 도 뺄 수 있다. 반대로 **글자 혼입이 다시 잦아지면** 전량 강화가
아니라 `--escalate text` 를 실패 자산에만 붙이는 현재 방식을 먼저 확인한다.

---

## 3. 왜 규격 책임을 임포터(`import-images.mjs`)로 옮겼나

**결정.** 어떤 크기·비율·포맷으로 받아도 임포터가 ① 단색 테두리 트림 ② 목표 규격
cover 크롭(단어=주목영역, 예문=위쪽 우선) ③ PNG 변환 ④ 의심 경고(과다 크롭·업스케일·
단색 비율)를 한다. 렌더 게이트(`check-assets`)는 **최종 파일 규격만** 본다.

**근거.**
- 2번 결정의 필연적 결과다. 프롬프트에서 형식을 뺐으니 형식을 맞출 곳이 필요하다.
- 실측: 1024×1024 이미지의 위아래를 단색으로 채운 "16:9 실패작" 을 넣었더니
  **단색 테두리 44% 제거 → 1600×900 크롭** 으로 정상 복구됐고, 피사체와 하단 자막
  여백이 살아남았다.
- 예문 이미지는 하단 1/4 에 자막 바가 깔린다. 그래서 크롭 기준을 **북쪽(위쪽 우선)**
  으로 고정했다. 프롬프트의 "upper two thirds" 한 구절은 이걸 돕는 최소 장치다.
- 백엔드 교체 비용이 0 이 된다. Codex·Google Flow·nano banana·agy·손그림이 전부
  `inbox/` 하나로 수렴한다. v1 은 백엔드마다 규격 변환 코드가 따로 있어 늘리기 어려웠다.

**버린 대안.** 백엔드별 변환 로직(v1 방식) — 도구를 늘릴수록 분기가 늘어난다.

**주의.** 임포터는 **고치지 못하는 것**이 있다. 의미 불일치, 글자 혼입, 무서운 그림은
사람이 본다. 그래서 경고는 자동 수정하지 않고 보고만 한다.

---

## 4. 왜 같은 범위를 여러 AI 에게 시키고 고르나

**결정.** 1단계는 여러 AI(Opus·Sol·Gemini 등)가 **같은 세트를 각각** 만들어
`content/candidates/<author>/` 에 쌓고, 비교 후 세트별로 하나를 승격한다.
2단계 이미지도 AI별로 `content/image-candidates/<author>/` 에 쌓고 3단계에서 고른다.

**근거.**
- **기계가 검증할 수 없는 영역이 여기에 몰려 있다.** IPA 정확도, 정의의 의미적 타당성,
  예문의 자연스러움, 이미지의 의미 적합성은 어떤 스크립트도 못 잡는다.
  `validate-content.mjs` 가 잡는 것은 형식(단어 수, 파일명, 화자 순환, CSV 일치)뿐이다.
- **회복 비용이 가장 큰 오류다.** 잘못된 발음기호를 아이가 외우면 영상을 다시 만들어도
  되돌릴 수 없다. 동형이음어(`wound`, `live`, `read`, `tear`, `close`, `lead`, `bow`)가
  대표적이며, v1 에서도 `wound /wuːnd/` 를 손으로 고정해야 했다.
- 후보가 여러 벌이면 **차이가 나는 지점이 곧 의심 지점**이다. `compare-candidates.mjs`
  가 단어별 IPA·뜻·정의·예문을 나란히 놓는 이유다.

**버린 대안.**
- 한 AI 가 순서대로 전부(v1 방식) → 그 AI 의 편향이 120세트에 그대로 박힌다.
- 역할 분담(A 는 콘텐츠, B 는 프롬프트) → 순차 의존이 생겨 병렬이 안 된다.

**바꿔도 되는 조건.** DAY 01~10 을 여러 벌로 만들어 품질 차이가 유의미하지 않으면
나머지는 1~2벌로 줄인다. 승격 스크립트는 후보가 1벌이어도 동작한다.

---

## 5. 왜 생성과 검증을 다른 AI 에게 시키나

**결정.** `요청문/3_검증.txt` 는 **만든 AI 가 아닌 다른 AI** 에게 준다. 검증 AI 에게는
수정 권한을 주지 않고, 보고 형식(반드시 고쳐야 함 / 고치면 좋음 / 애매 / 이상 없음)을
고정한다. 수정은 원래 만든 쪽이나 사람이 한다.

**근거.** 만든 모델은 자기가 쓴 기준으로 자기 산출물을 본다. 자기 정의가 초6 수준인지,
자기 IPA 가 맞는지에 대해 같은 판단을 반복한다. 또 검증과 수정을 같은 세션에서 하면
"고치면서 검증했다" 는 보고가 나와 무엇이 바뀌었는지 추적이 안 된다.

**바꿔도 되는 조건.** 없음에 가깝다. 다만 사람이 직접 검토한다면 이 규칙은 불필요하다.

---

## 6. 왜 세션 상한을 문서가 아니라 코드로 막나

**결정.** `remotion/scripts/session-limits.mjs` 가 1회 실행 상한을 강제한다 —
1단계 DAY 10개, 2단계 이미지 80장(`--max` 최대 200), 3단계 선별 20세트.
4단계(TTS·렌더)만 무제한이다.

**근거 (전부 실제 사고).**
- v1 에서 `"DAY 20~25 까지 한번에 전부 끝까지 묻지말고 해줘"` 요청이 있었다. 결과는
  콘텐츠만 쌓이고 이미지·음성은 손도 못 댄 채 51분을 쓰고 중단이었다.
- 이미지 단계는 외부 쿼터에서 끊긴다. v1 DAY18 에서 agy 가 13장 만들고
  `429 RESOURCE_EXHAUSTED`, 리셋 예상 5시간이었다.
- **끊긴 뒤 "어디까지 됐는지" 를 복원하는 비용이 작업 자체보다 크다.**
- 문서에만 적으면 에이전트가 무시한다. v1 은 AGENTS.md 에 "한 세션 표준 분량은 DAY
  1개" 라고 적어 뒀지만 지켜지지 않았다.

**설계 원칙으로 일반화하면:** *지켜야 하는 규칙은 문서가 아니라 게이트로 만든다.*
`validate-content` → 승격 → `check-assets` → 렌더의 게이트 순서도 같은 원칙이다.

---

## 7. 왜 git 에 텍스트만 넣나

**결정.** 추적하는 것은 `words.json`, `image_prompts.md`, 절차 문서, 파이프라인 코드,
`samples/` 예시 3개뿐이다. 이미지·음성·영상·`node_modules` 는 전부 제외한다.

**근거.**
- 자산은 **프롬프트와 스크립트로 다시 만들 수 있다.** 텍스트가 원본이고 자산은 파생물이다.
- v1 은 자산을 추적해서 DAY01 만으로 저장소가 190MB 였고, 30일이면 3GB 였다.
- 영상은 세트당 55MB × 120편 = 6.6GB 다. git 이 다룰 물건이 아니다.

**따라오는 규칙 (중요).** 클론한 저장소에는 이미지가 없다. **저장소에 이미지가 없다고
"아직 안 만들었다" 고 판단하면 틀린다.** 진행 상황의 유일한 근거는 로컬 디스크를 읽는
`node remotion/scripts/status.mjs` 다. 이 오해는 실제로 사고를 냈다(아래 9번).

---

## 8. 왜 타이밍 격자·화자 순환·속도 정책은 못 건드리나

**결정.** 단어당 19초 격자(1.5/1.5/8/8), 예문 화면 1.5초 선행, narration 정확히 190.0초,
화자 순환 Zephyr→Liam→Erinome→Charon, 속도 보정 상수(압축 하한 0.85 / 감속 상한 1.2 /
3.0→2.6 단어per초)는 **사용자 확정값**이며 지시 없이 바꾸지 않는다.

**근거.**
- 120편이 같은 리듬이어야 학습 도구로 쓸 수 있다. 중간에 바꾸면 그 이전 산출물이 전부
  구버전이 된다.
- 이 값들은 이미 실측으로 조정된 결과다. 정의 12단어 제한도 "느린 낭독이 8초 슬롯에
  안전하게 들어가도록" v1 에서 15 → 12 로 강화한 것이다.
- overflow 가 나면 **격자가 아니라 콘텐츠를 고친다.** 이게 이 설계의 방향이다.

---

## 9. 왜 작업 폴더 경고가 문서 맨 위에 있나

**결정.** `AGENTS.md` 첫 화면에 작업 폴더와 읽기 전용 폴더를 못박고, 모든 요청문에
공통 서두 4줄을 붙인다.

**근거 (실제 사고, 2026-08-17).** Codex 세션에 "이 폴더의 내용을 읽고 알아서 비디오를
만들라" 고 했더니 **구프로젝트(`junbe_study/voca_video_plan`)에서 작업했다.** DAY21~25
콘텐츠 20세트와 스크립트 1개를 그 저장소에 새로 쓰고, "Qwen TTS 자동화가 연결돼 있지
않다" 고 정확하지만 **엉뚱한 폴더에 대해** 보고했다. 51분과 토큰이 버려졌고 산출물은
스키마가 달라 재사용도 못 했다.

**교훈 두 가지.** ① 에이전트에게 "알아서" 를 시키려면 경계를 먼저 못박아야 한다.
② 에이전트의 보고는 **자기가 본 것에 대해서만** 참이다. 어느 폴더를 봤는지 확인해야 한다.

---

## 10. 검토했다 버린 대안 모음

| 대안 | 왜 버렸나 |
|---|---|
| 로컬 이미지 생성(ComfyUI 등) | 사용자가 직접 써 보고 품질 미달로 결론. 이 PC 에 설치돼 있어도 다시 제안하지 않는다 |
| 안티그래비티 IDE 를 MCP 로 연결 | Electron IDE 에 서버 모드가 없다. 단 `agy` **CLI** 는 별개이며 쓴다 |
| `agy` 로 TTS | 이미지 전용이다(2026-08-11 실측). 세션 도구에 생성 계열은 `generate_image` 하나뿐 |
| 외부 TTS + SRT 로 타이밍 역추출 (구프로젝트 방식) | 우리가 세그먼트를 직접 만들면 타이밍이 결정적이 된다. SRT·전사·`timing.json`·토큰 보간 QA가 전부 사라졌다 |
| `words.json` 에 `author` 필드 추가 | Remotion props 스키마가 `.strict()` 라 렌더가 거부한다. 후보 구분은 폴더명으로 한다 (실제로 스모크에서 잡힌 사고) |
| 프롬프트에 강화 금지문구 전량 적용 | 2번 참조. 프롬프트가 길어질수록 약한 모델이 무너진다 |

---

## 11. 아직 안 풀린 것 (검토 세션이 볼 만한 지점)

- **이미지 2,400장의 수급 경로가 미확정이다.** agy 는 쿼터, Codex 는 순차 60~90초/장,
  웹 UI 는 사람 손이 든다. 2단계 전체 소요를 아무도 실측하지 않았다.
- **선별(3단계)의 인건비.** 세트당 20장 × 120세트를 사람이 본다. 대지(contact sheet)로
  줄였지만 여전히 크다. 자동 1차 필터(글자 검출 등)를 넣을 여지가 있다.
- **4단계 총 소요는 추정치다** — TTS 약 3.8시간 + 렌더 약 4.6시간(120세트 콜드 환산).
  1세트 실측은 TTS RTF 1.786, 렌더 147초였다.
- **원격 저장소가 없다.** v2 는 로컬 git 만 있다.
- **v1 의 GitHub 이력에 자산 190MB 가 남아 있다.** v1 을 지우기로 했다면 그 저장소를
  어떻게 할지 결정이 필요하다.

---

## 12. 새 세션이 프로세스를 검토할 때

읽는 순서: **이 문서 → `PLAN.md` → `AGENTS.md` → `prompts/P1`~`P4` → `handover.md` 최신 항목**.
그다음 `node remotion/scripts/status.mjs` 로 실제 상태를 본다 (git 이 아니라 로컬 디스크가
진실이다).

검토는 아래 질문에 답하는 형태가 좋다.

1. 이 설계가 못 막는 실패는 무엇인가? (게이트가 없는 지점, 사람만 잡을 수 있는 지점)
2. 근거가 낡은 결정은 없나? 특히 **2번(프롬프트 단순화)** 은 사용하는 이미지 모델이
   바뀌면 근거가 바뀐다
3. 단계 경계가 맞나? 사람이 개입해야 하는 지점이 정말 3곳(승격·선별·최종 QA)뿐인가
4. 상한 수치(DAY 10개 / 80장 / 20세트)가 실제 세션 한계와 맞나
5. 자동화할 수 있는데 사람이 하고 있는 일은 무엇인가

**검토 결과를 낼 때는 결정을 바꾸자고만 하지 말고, 위 각 항목의 "근거" 를 무엇으로
대체할지 함께 써라.** 근거 없는 변경은 이 문서에 반영하지 않는다.
바뀐 결정은 이 문서에 **날짜와 함께 항목을 갱신**하고 `handover.md` 에도 남긴다.

복붙용 검토 요청문: `요청문/4_프로세스_검토.txt`
